
    <header class="hero">
        <h1>Construire le futur.</h1>
        <p>L'espace communautaire dédié à l'électronique, au code et à la fabrication numérique.</p>
        <a href="https://discord.gg/JUmaNx7mqJ" class="btn-discord">Rejoindre le Hub</a>
    </header>


<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Maker Hub | Communauté Tech</title>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        :root {
            /* Palette Grise / Mode Clair */
            --bg: #f5f5f7;
            --text: #1a1a1a;
            --primary: #334155; /* Gris Ardoise foncé */
            --secondary: #94a3b8;
            --card-bg: #ffffff;
            --nav-bg: rgba(245, 245, 247, 0.8);
            --accent: #64748b;
        }

        [data-theme="dark"] {
            /* Palette Grise / Mode Sombre (Style Chat/Discord) */
            --bg: #0f1113;      /* Fond très sombre */
            --text: #e2e8f0;    /* Texte gris clair */
            --primary: #94a3b8; /* Gris clair pour les accents */
            --secondary: #475569;
            --card-bg: #1e2023; /* Cartes gris foncé */
            --nav-bg: rgba(15, 17, 19, 0.9);
            --accent: #cbd5e1;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; transition: all 0.3s ease; }
        body { font-family: 'Inter', -apple-system, sans-serif; background: var(--bg); color: var(--text); line-height: 1.6; }

        /* Navigation */
        nav { position: fixed; width: 100%; top: 0; background: var(--nav-bg); backdrop-filter: blur(12px); z-index: 1000; padding: 0.8rem 5%; display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid var(--secondary); }
        
        .logo-container { display: flex; align-items: center; gap: 12px; text-decoration: none; }
        .logo-img { 
            width: 40px; 
            height: 40px; 
            object-fit: contain; 
            border-radius: 8px; /* Optionnel: arrondit ton logo */
        }
        .logo-text { font-weight: 800; font-size: 1.4rem; color: var(--text); letter-spacing: -0.5px; }

        .nav-links { display: flex; gap: 2rem; list-style: none; }
        .nav-links a { text-decoration: none; color: var(--text); font-weight: 500; font-size: 0.95rem; }
        .nav-links a:hover { color: var(--primary); }

        .theme-toggle { cursor: pointer; background: var(--card-bg); border: 1px solid var(--secondary); color: var(--text); padding: 8px; border-radius: 10px; display: flex; align-items: center; }

        nav {
            position: fixed;
            width: 100%;
            top: 0;
            background: var(--nav-bg);
            backdrop-filter: blur(12px);
            z-index: 1000;
            padding: 0.8rem 2%; /* Réduit à 2% pour coller plus aux bords */
            display: flex;
            align-items: center;
            justify-content: space-between; /* Espace logo / menu / bouton */
            border-bottom: 1px solid var(--secondary);
        }

        /* On force les liens au centre exact */
        .nav-links {
            display: flex;
            gap: 2rem;
            list-style: none;
            position: absolute; /* Sort le menu du flux normal */
            left: 50%;         /* Se place au milieu */
            transform: translateX(-50%); /* Se décale de sa propre moitié pour être pile au centre */
        }

        /* On s'assure que le bouton reste bien à droite */
        #themeBtn {
            margin-left: auto; /* Pousse le bouton vers la droite si besoin */
        }

        /* Sections & Animations */
        section { padding: 120px 10%; opacity: 0; transform: translateY(20px); }
        section.visible { opacity: 1; transform: translateY(0); }
        
        .hero { text-align: center; padding: 180px 10% 120px; }
        .hero h1 { font-size: 4rem; font-weight: 900; margin-bottom: 1rem; color: var(--text); }
        .hero p { font-size: 1.2rem; color: var(--secondary); max-width: 600px; margin: 0 auto; }
        
        .btn-discord { background: #5865F2; color: white; padding: 14px 28px; border-radius: 12px; text-decoration: none; display: inline-block; margin-top: 2rem; font-weight: 600; box-shadow: 0 4px 15px rgba(88, 101, 242, 0.3); }
        .btn-discord:hover { transform: translateY(-3px); box-shadow: 0 6px 20px rgba(88, 101, 242, 0.4); }

        /* Grilles & Cartes */
        .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1.5rem; margin-top: 3rem; }
        .card { background: var(--card-bg); padding: 2.5rem; border-radius: 16px; border: 1px solid rgba(148, 163, 184, 0.1); position: relative; overflow: hidden; }
        .card:hover { border-color: var(--primary); transform: translateY(-5px); }
        .card h3 { margin-bottom: 1rem; color: var(--accent); }

        /* Staff Section */
        .staff-card { text-align: center; }
        .staff-avatar { width: 80px; height: 80px; background: var(--secondary); border-radius: 50%; margin: 0 auto 1.5rem; display: flex; align-items: center; justify-content: center; font-size: 1.5rem; }

        footer { text-align: center; padding: 4rem 2rem; border-top: 1px solid var(--secondary); opacity: 0.7; font-size: 0.9rem; }

        @media (max-width: 768px) {
            .nav-links { display: none; }
            .hero h1 { font-size: 2.5rem; }
        }

        /* Espaces sous les titres */

        section h2 {
            margin-bottom: 2rem; /* Ajuste cette valeur (1.5rem, 2rem, 3rem) selon tes goûts */
            font-size: 2rem;
            font-weight: 700;
        }

        /* Texte qui défile */

        .cursor {
            font-weight: 300;
            color: var(--primary);
            animation: blink 1s infinite;
            margin-left: 5px;
        }

        @keyframes blink {
            0%, 100% { opacity: 1; }
            50% { opacity: 0; }
        }
    </style>
</head>
<body data-theme="dark"> <nav>
        <a href="#" class="logo-container">
            <img src="images/logo.png" alt="Logo" class="logo-img">
            <span class="logo-text">MAKER HUB</span>
        </a>
        <ul class="nav-links">
            <li><a href="#presentation">Concept</a></li>
            <li><a href="#blog">Blog</a></li>
            <li><a href="#formation">Formation</a></li>
            <li><a href="#staff">Staff</a></li>
        </ul>
        <button class="theme-toggle" id="themeBtn"><i data-lucide="sun"></i></button>
    </nav>

    <header class="hero">
        <h1><span id="typewriter"></span><span class="cursor">|</span></h1>
        <p>L'espace communautaire dédié à l'électronique, au code et à la fabrication numérique.</p>
        <a href="https://discord.gg/tonlien" class="btn-discord">Rejoindre le Hub</a>
    </header>


    <!-- SECTION : Présentation -->

    <section id="presentation">
        <h2>Le Projet</h2>
        <div class="grid">
            <div class="card">
                <i data-lucide="cpu" style="margin-bottom:1rem"></i>
                <h3>Électronique</h3>
                <p>De la conception de circuits à la programmation de micro-contrôleurs.</p>
            </div>
            <div class="card">
                <i data-lucide="settings" style="margin-bottom:1rem"></i>
                <h3>Ingénierie</h3>
                <p>Partage de connaissances théoriques et pratiques en mécanique et système.</p>
            </div>
            <div class="card">
                <i data-lucide="wrench" style="margin-bottom:1rem"></i>
                <h3>Maker Culture</h3>
                <p>Impression 3D, CNC, bois et métal. L'art de fabriquer soi-même.</p>
            </div>
        </div>
    </section>

    <!-- SECTION : Blog -->

    <section id="blog">
        <h2>Articles & Projets</h2>
        <div class="grid">
            <div class="card">
                <small>PROJET EN COURS</small>
                <h3>Station météo LoRa</h3>
                <p>Comment nous avons déployé une sonde autonome sur 10km.</p>
            </div>
            <div class="card">
                <small>TUTO</small>
                <h3>Optimiser ses impressions 3D</h3>
                <p>Réglages fins pour des pièces mécaniques résistantes.</p>
            </div>
        </div>
    </section>

    <!-- SECTION : Apprentissage -->

    <section id="formation">
        <h2 style="margin-bottom: 2rem;">Apprentissage</h2>
        <div class="stack" style="display: flex; flex-direction: column; gap: 20px;"> 
            
            <div class="card">
                <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 10px;">
                    <h3>Workshop hebdomadaire</h3>
                    <span style="background: var(--secondary); color: var(--bg); padding: 4px 12px; border-radius: 20px; font-size: 0.8rem; font-weight: bold;">LIVE BIENTÔT</span>
                </div>
                <p>Chaque mercredi soir, un membre présente une technologie ou un projet spécifique en vocal.</p>
            </div>

            <div class="card">
                <h3>Initiation à la soudure SMD</h3>
                <p>Apprenez à souder des composants minuscules sans trembler. Matériel requis : une loupe et de la patience !</p>
            </div>

        </div>
    </section>

    <section id="staff">
        <h2>L'Équipe</h2>
        <div class="grid">
            <div class="card staff-card">
                <div class="staff-avatar">👨‍💻</div>
                <h4>Admin_Hub</h4>
                <p>Ingénieur Électronique</p>
            </div>
            <div class="card staff-card">
                <div class="staff-avatar">🛠️</div>
                <h4>Maker_Master</h4>
                <p>Expert Impression 3D</p>
            </div>
        </div>
    </section>

    <footer>
        <p>Maker Hub &copy; 2026 - Propulsé par la curiosité.</p>
    </footer>

    <script>
        lucide.createIcons();

        const themeBtn = document.getElementById('themeBtn');
        const body = document.body;
        
        // Système de switch
        themeBtn.addEventListener('click', () => {
            const currentTheme = body.getAttribute('data-theme');
            const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
            body.setAttribute('data-theme', newTheme);
            themeBtn.innerHTML = newTheme === 'dark' ? '<i data-lucide="sun"></i>' : '<i data-lucide="moon"></i>';
            lucide.createIcons();
        });

        // Animation d'apparition
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) entry.target.classList.add('visible');
            });
        }, { threshold: 0.1 });

        document.querySelectorAll('section').forEach(s => observer.observe(s));

        // Texte qui défile

        const textElement = document.getElementById('typewriter');
        const phrases = [
            "Construire le futur...",
            "Concevoir...",
            "Créer des projets...",
            "Rejoindre Maker Hub..."
        ];

        let phraseIndex = 0;
        let charIndex = 0;
        let isDeleting = false;
        let typeSpeed = 2500;

        function type() {
            const currentPhrase = phrases[phraseIndex];
            
            if (isDeleting) {
                textElement.textContent = currentPhrase.substring(0, charIndex - 1);
                charIndex--;
                typeSpeed = 50; // Plus rapide quand on efface
            } else {
                textElement.textContent = currentPhrase.substring(0, charIndex + 1);
                charIndex++;
                typeSpeed = 150; // Vitesse d'écriture normale
            }

            if (!isDeleting && charIndex === currentPhrase.length) {
                isDeleting = true;
                typeSpeed = 2000; // Pause à la fin de la phrase
            } else if (isDeleting && charIndex === 0) {
                isDeleting = false;
                phraseIndex = (phraseIndex + 1) % phrases.length;
                typeSpeed = 500; // Pause avant de recommencer
            }

            setTimeout(type, typeSpeed);
        }

        // Lancement de l'animation
        document.addEventListener('DOMContentLoaded', type);
    </script>
</body>
</html>